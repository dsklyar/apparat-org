export { Button, Text, Tabs } from "./src/components";
export { GlobalStyles } from "./src/styles";
export { ArrowIcon } from "./src/assets";

/**
 * TODO:
 * Make sure peer dependencies of styled components react react dom are matching in the library consumer applications
 * as well as the library itself 
 * in order to prevent addidtional styled-dcomponents install
 */